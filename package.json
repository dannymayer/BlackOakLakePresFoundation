{
  "name": "blackoaklakepresfoundation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx @11ty/eleventy --serve && npx tailwindcss -i ./src/css/style.css -o ./_site/css/style.css --watch",
    "build:eleventy": "npx @11ty/eleventy",
    "build:tailwind": "npx tailwindcss -i ./src/css/style.css -o ./_site/css/style.css --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "tailwindcss": "^3.3.2"
  }
}